
<h1>
    Juego Aleatorio:
</h1>

<div>
    Preguntas acertadas = <span id="score"><%= score %></span>
</div>

<p>
    La respuesta <strong> <%= answer %> </strong> a la pregunta con id <%= id %> es <%= result ? 'Correcta' : 'Incorrecta' %>.
</p>

<p>
    <% if (!result) { %>
    Has fallado.
    <a href="/quizzes"> FIN </a>
    <% } else { %>
    <% // Metemos la contestada en el array %>
    <% contestadasResult.unshift(id)%>
    Has acertado.
    <a href="/quizzes/randomplay"><button>Continuar jugando</button></a>
    <% } %>
</p>

